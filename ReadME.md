Git commands:

git init

subl add.java // to add a file to the remote folder

git add .

git config --global user.email "YOUR EMAIL"

git config --global user.name "YOUR NAME"

git remote add origin https://github.com/crossfire729/CapTestNew.git

git status

git add .

git commit -m "!st commit"

git push -u origin master 	

------------------

git pull origin master